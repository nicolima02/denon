Para levantar el servidor se debe ejecutar el comando denon start en la consola.

para agregar un color a la lista se debe hacer un post en http://localhost:8080 con los parametros: "color": "(color que se quiera)"

para ver la lista de colores se debe borrar los parametros e ir a la ruta http://localhost:8080 con el metodo GET